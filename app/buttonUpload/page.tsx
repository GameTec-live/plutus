import { ButtonUploader } from "@/components/upload/buttonUploader";

export default function Page() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center">
            <ButtonUploader />
        </main>
    );
}
