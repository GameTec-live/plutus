query GetProjectBalanceByProjectId($slug: String) {
    project(slug: $slug) {
        stats {
            id
            balance {
                valueInCents
                currency
            }
        }
    }
}
